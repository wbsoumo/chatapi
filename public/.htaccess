RewriteEngine On

# Allow direct access to uploaded files or assets
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Redirect all other requests to index.php
RewriteRule ^ index.php [L,QSA]
